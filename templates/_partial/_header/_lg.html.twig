{#    desktop hidden on mobile=============================================== #}
<div class="hidden max-w-7xl m-auto lg:grid grid-cols-12 ">
    <div id="brand" class="col-span-3  text-left flex !items-baseline space-x-4 text-content-secondary/70">
        {% if globalStore is defined %}
            {% if globalStore.logoName is not null %}
                <a href="{{ path('app_menu', {slug: globalStore.slug}) }}"
                   class="w-24 aspect-video">
                    <img src="{{ asset('images/stores/' ~ globalStore.logoName) }}"
                         class="w-full"
                         alt="{{ globalStore.name }} |Snapmenu.be">
                </a>
            {% else %}
                <div class="capitalize">{{ globalStore.name }}</div>
            {% endif %}
        {% else %}
            <a href="{{ path('app_default') }}"> Snap<strong>Menu</strong> </a>
        {% endif %}
        {# {% if globalStore.name is defined %}
                    <twig:Title:H1 class="capitalize">{{ globalStore.name }}</twig:Title:H1>
                {% endif %}
                <a href="{{ path('app_default') }}"> Snap<strong>Menu</strong> </a> #}
    </div>
    <nav class="col-span-7 text-base overflow-hidden h-full flex justify-start !items-baseline">
        <ul class="h-full flex  justify-start  items-center gap-x-4 m-auto">
            <li>
                <a href="{{ path('app_default') }}">{% trans %}nav.home {% endtrans %}</a></li>
            <li>
                <a href="{{ path('app_menu', {'slug': globalStore.slug, '_fragment': 'menu'}) }}">{% trans %}nav.menu {% endtrans %}</a>
            </li>
            <li>
                <a href="{{ path('app_menu', {'slug': globalStore.slug, '_fragment': 'heures-d-ouverture'}) }}">{% trans %}nav.opening {% endtrans %}</a>
            </li>
            <li>
                <a href="{{ path('app_menu', {'slug': globalStore.slug, '_fragment': 'contact'}) }}">{% trans %}nav.contact_infos {% endtrans %}</a>
            </li>

            <li class="ml-4 opacity-80">
                {% if app.user is null %}
                    <a href="{{ path('app_login') }}"> {% trans %} nav.login{% endtrans %}</a>
                {% else %}
                    <a href="{{ path('admin_default') }}">  {% trans %} nav.admin {% endtrans %}</a>
                    <a href="/logout">  {% trans %} nav.logout {% endtrans %}</a>µ
                {% endif %}
            </li>

        </ul>

    </nav>
    <div class="col-span-2 p-1 text-right text-base">
        {% if globalStore is defined %}
            <a href="{{ path('app_menu', {'slug': globalStore.slug, '_fragment': 'contact'}) }}"
                    {#            <a href="{{ path('app_contact', {slug: globalStore.slug}) }}" #}
               class="w-fit inline-flex justify-center items-center px-8 py-4  text-content-primary-light border border-dark-content-text-dark dark:border-content-text-light/70 rounded-md">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                     stroke="currentColor" class="w-8 h-8">
                    <path stroke-linecap="round" stroke-linejoin="round"
                          d="M3.75 4.875c0-.621.504-1.125 1.125-1.125h4.5c.621 0 1.125.504 1.125 1.125v4.5c0 .621-.504 1.125-1.125 1.125h-4.5A1.125 1.125 0 0 1 3.75 9.375v-4.5ZM3.75 14.625c0-.621.504-1.125 1.125-1.125h4.5c.621 0 1.125.504 1.125 1.125v4.5c0 .621-.504 1.125-1.125 1.125h-4.5a1.125 1.125 0 0 1-1.125-1.125v-4.5ZM13.5 4.875c0-.621.504-1.125 1.125-1.125h4.5c.621 0 1.125.504 1.125 1.125v4.5c0 .621-.504 1.125-1.125 1.125h-4.5A1.125 1.125 0 0 1 13.5 9.375v-4.5Z"/>
                    <path stroke-linecap="round" stroke-linejoin="round"
                          d="M6.75 6.75h.75v.75h-.75v-.75ZM6.75 16.5h.75v.75h-.75v-.75ZM16.5 6.75h.75v.75h-.75v-.75ZM13.5 13.5h.75v.75h-.75v-.75ZM13.5 19.5h.75v.75h-.75v-.75ZM19.5 13.5h.75v.75h-.75v-.75ZM19.5 19.5h.75v.75h-.75v-.75ZM16.5 16.5h.75v.75h-.75v-.75Z"/>
                </svg>
            </a>
        {% endif %}
    </div>
</div>
{# end desktop -------------------------------------------------------------- -#}