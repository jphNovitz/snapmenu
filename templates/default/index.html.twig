{% extends 'base.html.twig' %}

{% block title %}Snapmenu:
    Un QR code, tout le menu | snapmenu.be{% endblock %}

{% block body %}
    {#    <div class="absolute -top-[45vw] top-0 left-1/2 -translate-x-1/2 clear-gradient #}{# dark-gradient #}{# #}{# bg-gradient-to-b from-secondary via-clear-light to-clear #}{# block w-[50vw] h-[50vw] #}{# opacity-7 #}{#  shadow-clear-dark shadow-2xl aspecct-square rounded-full"> #}

    {#    </div> #}

    {#    <div class="relative"> #}

    <div class="my-12 md:my-16 lg:my-24  w-10/12 lg:w-2/3 m-auto flex items-center justify-center relative  ">
        {#            <div class="absolute  -z-10 w-1/2 h-1/5 left-1/2 -translate-x-1/2 top-1/5 -translate-y-1/5  shadow-clear-dark shadow-2xl rounded-full"></div> #}
        {#        Scannez. Commandez. Savourez. #}
        {#        Votre menu en un scan #}
        {#        Découvrez le menu en un clic #}
        {#        Scannez et régalez-vous #}
        {#        Un QR code, tout le menu #}

        <div class="p-6 md:p-12 bg-clear dark:bg-primary-dark relative after:absolute after:content-[' '] after:-z-10 {# after:w-11/12 #} after:inset-x-[1rem] {# after:h-11/12 #} after:inset-y-[1rem]  after:shadow-secondary dark:after:shadow-clear-dark after:shadow-xl after:rounded-xl">
            <h1 class="text-3xl lg:text-5xl font-black leading-relaxed capitalize">Snapmenu:<br/> Un QR code, tout le
                menu</h1>
            <h2 class="my-12 text-xl lg:text-2xl font-bold leading-relaxed">Dites adieu aux menus papier. Scannez le
                code QR pour
                découvrir le
                menu
                complet et commandez directement depuis un téléphone.</h2>

            <div class="flex flex-col md:flex-row space-y-8  md:space-x-8 md:space-y-0 md:justify-center">
                <a href="{{ path('app_login') }}"
                   class="w-full md:w-auto px-4 py-2 text-xl font-semibold text-clear  bg-secondary border border-secondary-dark rounded-lg text-center z-100">
                    Je rejoins Snapmenu
                </a>
                <a href="{{ path('app_default', {'_fragment': 'list'}) }}"
                   class="w-full md:w-auto px-4 py-2  text-xl font-semibold border text-secondary-dark dark:text-clear border-secondary-dark  dark:border-clear  rounded-lg text-center">
                    Voir les Restaurants
                </a>
            </div>
        </div>
    </div>
    {#    </div> #}

    <div class="min-h-screen flex flex-col justify-evenly ">
        <div>
            <h2 class="text-xl md:text-2xl font-bold leading-loose">
                Encodez votre liste de produit
            </h2>
            <h3 class="my-4 text-lg">
                Donnez-lui un nom, un prix sur place, un prix emporté.
            </h3>
        </div>
        <img src="{{ asset('images/screenshots/screenshot_list.png') }}"
             alt="Encodez votre liste de produit"
             class="border-clear-dark aspect-video">
    </div>
    <div class="w-full min-h-screen flex flex-col justify-evenly">

        <h2 class="text-xl md:text-2xl font-bold leading-loose">Affichez le code QR dans votre établissement</h2>

        <h3 class="my-4 text-lg">Les clients n'ont qu'à scanner ce code avec leur téléphone pour afficher :</h3>

        <ul class="ml-24 space-y-12 font-semibold flex flex-col">
            <li class="inline-flex">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                     stroke="currentColor" class="size-6">
                    <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5"/>
                </svg> &nbsp;
                Votre menu
            </li>
            <li class="inline-flex">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                     stroke="currentColor" class="size-6">
                    <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5"/>
                </svg> &nbsp;
                Vos heures d'ouverture
            </li>
            <li class="inline-flex">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                     stroke="currentColor" class="size-6">
                    <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5"/>
                </svg> &nbsp;
                Vos informations de contact
            </li>
        </ul>
        <div class="leading-loose">
            <p>En cas de modification rendez-vous sur votre interface d'administration, le code ne change pas:
                inutile de
                réimprimer des menus. </p>
            <p>Vos visiteurs deviennent des client et les clients deviennent des embassadeurs car il peuvent faire
                scanner
                le code pour le partager avec leurs amis.</p>

        </div>
    </div>

    <div class=" my-8 grid grid-cols-2 md:grid-cols-3 relative">
        <div>
            <img src="{{ asset('images/screenshots/screenshot_contact.jpg') }}"
                 alt="Encodez votre liste de produit"
                 class="border border-4 border-clear-dark aspect-square">
        </div>
        <div>
            <img src="{{ asset('images/screenshots/screenshot_menu.png') }}"
                 alt="Encodez votre liste de produit"
                 class="border border-4 border-clear-dark aspect-square">
        </div>
        <div class="col-span-2 md:col-span-1 text-center">
            <img src="{{ asset('images/screenshots/screenshot_opening.png') }}"
                 alt="Encodez votre liste de produit"
                 class="border border-4 border-clear-dark aspect-square">
        </div>
        <h2 class="my-4 p-4 text-xl lg:text-2xl font-bold leading-loose absolute bg-clear-light/80 text-primary-dark left-1/2 top-1/2 -translate-x-1/2  -translate-y-3/4 ">
            Vous êtes prêts à être scannés</h2>
    </div>


    <div class="my-12 md:my-16 lg:my-24 min-h-screen" id="list">
        <h2 class="my-24 text-xl lg:text-2xl font-bold leading-loose">✅ Ces restaurateurs Snappent !</h2>
        <div class="my-24 w-full grid grid-cols-1 md:grid-cols-2  lg:grid-cols-3 gap-x-4 gap-y-8 ">

            {% for store in stores %}
                <div class="w-full flex flex-col justify-center items-center aspect-square dark:bg-clear-light">
                    <a href="{{ path('app_menu', {slug: store.slug}) }}"
                       class="w-2/3">
                        <img src="{{ asset('images/stores/' ~ store.logoName) }}"
                             class="w-full"
                             alt="{{ store.name }} |Snapmenu.be">
                    </a>
                    <a href="{{ path('app_menu', {slug: store.slug}) }}"
                       class="text-xl p-2 dark:text-primary-dark relative overflow-hidden after:w-full after:absolute after:bottom-0 after:-left-36 hover:after:left-0 after:content-[' '] after:border transition-positions">
                        {{ store.name }}
                    </a>
                </div>
            {% else %}
                {% trans %} text.store.no_store{% endtrans %}
            {% endfor %}
        </div>
    </div>
{% endblock %}

