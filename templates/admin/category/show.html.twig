{% extends 'base.html.twig' %}

{% block title %}CategoryCustom{% endblock %}

{% block body %}
    <h1>CategoryCustom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_custom.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_custom.name }}</td>
            </tr>
            <tr>
                <th>Pos</th>
                <td>{{ category_custom.pos }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_category_index') }}">back to list</a>

    <a href="{{ path('admin_category_edit', {'id': category_custom.id}) }}">edit</a>

    {{ include('admin/category/_delete_form.html.twig') }}
{% endblock %}
