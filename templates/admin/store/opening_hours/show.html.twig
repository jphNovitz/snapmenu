{% extends 'admin/base.html.twig' %}

{% block title %}OpeningHours{% endblock %}

{% block body %}
    <h1>OpeningHours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ opening_hour.id }}</td>
            </tr>
            <tr>
                <th>DayOfWeek</th>
                <td>{{ opening_hour.dayOfWeek }}</td>
            </tr>
            <tr>
                <th>OpenTime</th>
                <td>{{ opening_hour.openTime ? opening_hour.openTime|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CloseTime</th>
                <td>{{ opening_hour.closeTime ? opening_hour.closeTime|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_store_opening_hours_index') }}">back to list</a>

    <a href="{{ path('app_admin_store_opening_hours_edit', {'id': opening_hour.id}) }}">edit</a>

    {{ include('admin/store/opening_hours/_delete_form.html.twig') }}
{% endblock %}
